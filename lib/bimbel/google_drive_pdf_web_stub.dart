

import 'package:flutter/material.dart';

class GoogleDrivePdfWeb extends StatelessWidget {
  final String fileId;

  const GoogleDrivePdfWeb({
    super.key,
    required this.fileId,
  });

  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}